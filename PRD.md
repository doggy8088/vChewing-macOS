# PRD：智慧中英輸入自動切換（Smart ZH-EN Auto-Switch）MVP

- 文件狀態：草稿（MVP 施工中）
- 目標分支：`feat/smart-zh-en-auto-switch` 之二次開發
- 適用範圍：唯音輸入法（vChewing）macOS 版，注音鍵盤（大千等注音排列）語境
- 相關程式碼：`Packages/vChewing_OSNeutral_LibVanguard/Sources/LibVanguard/`

---

## 1. 基本假設

1. 使用者只安裝兩套輸入法：
   - **ABC**：英文輸入法（純英數）。
   - **繁唯**：唯音輸入法、且維持在中文輸入模式（非英數模式）。
2. 使用者已**關閉**「以 Shift 切換中文／英數模式」的快速鍵，避免誤觸英數模式而破壞心流。
3. 使用者切換中英一律在「ABC」與「繁唯」之間切換：想打中文就切到「繁唯」，想打英文就切到「ABC」。

## 2. 問題定義

使用者在不同應用程式之間頻繁跳轉（終端機、編輯器、瀏覽器……），經常忘記自己當下處於哪一套輸入法，導致：

- 在 ABC 輸入法底下試圖輸入中文（打不出注音）。
- 在「繁唯」底下試圖輸入英文命令（螢幕上出現注音符號或錯字）。

## 3. 解決方案（一句話）

假設使用者的常態是「停留在繁唯」，因此唯音本身要能**在同一套輸入法內**判斷「這串按鍵其實是英文」，自動把輸入結果轉為英文輸出，而不必切換到 ABC；一段時間未繼續輸入時，自動回到中文輸入。

## 4. 名詞定義

| 名詞 | 說明 |
|---|---|
| 中文模式 | 唯音當前的正常組字狀態（注音組字、選字窗等）。 |
| 英數暫存模式（Smart English Mode） | 本文新增的暫態模式：唯音**不離開自己**，但把使用者鍵入的可列印字元原樣輸出成英文（逐一遞交給客體 App），而非送進注音組字器。 |
| 輸入鍵序列（Trail） | 自上次組字內容被固化／遞交之後，使用者依序敲下的原始鍵。用來在需要時把「剛剛打的鍵」還原成英文字串。 |
| 合理性檢查 | 以注音語法模擬整段輸入鍵序列，判斷其是否為「合理的中文輸入順序」。 |

## 5. 行為規格

### 5.1 觸發條件（皆在「繁唯」中文模式下）

#### A. Tab 鍵：無條件轉英、並把真實 Tab 按鍵送給客體

- 情境：終端機的指令自動完成（tab-completion）、路徑輸入（例如 `./con` ＋ Tab、`conf` ＋ Tab）。
- 規則：中文模式下按 Tab，且**輸入鍵序列非空**時：
  1. 將序列中「尚未成文的中文組字內容」照常遞交（若有）。
  2. 將序列本身（原始 ASCII 鍵，**含 `.`、`/` 這類符號鍵**）逐字遞交為英文。
  3. 本拍 Tab **不被輸入法消費**：客體收到真實的 Tab 按鍵事件（keyDown／keyUp）而觸發自動完成；
     **不遞交 Tab 字元**（不會把 `\t` 當成文字插入），Tab 之後的輸入不受影響。
  4. 進入英數暫存模式。
- 備註：
  - 選字窗開啟中（`.ofCandidates`）或輸入鍵序列為空（例如剛打完一個完整讀音）時，Tab 維持既有行為（輪替候選等），不受本規則影響。
  - Shift+Tab（終端機的反向自動完成）一併視為觸發（同樣放行真實按鍵）；帶 Command／Control／Option 的 Tab 組合鍵則不觸發。
  - 進入英數暫存模式後再按 Tab（例如 `cd ` 之後自動完成下一段路徑）僅放行真實按鍵、不遞交任何內容、亦不結束模式。

#### B. Space 鍵：輸入順序不合理時轉英

- 情境：`cd `、`ls ` 這類指令。它們在注音鍵盤下分別對應 `ㄏㄎ `、`ㄠㄋ `，皆不是合理的中文輸入順序。
- 規則：中文模式下按 Space，且輸入鍵序列**被判定為不合理的中文輸入順序**（見 §5.2）時：
  1. 遞交尚未成文的中文組字內容（若有）。
  2. 遞交輸入鍵序列為英文。
  3. 遞交一個半形空格（本拍 Space 由唯音消費，不再走既有空格邏輯）。
  4. 進入英數暫存模式。
- 合理的中文輸入序列：完全維持既有行為（輪替、確認、遞交等），不受影響。
- **半形輸出保證**：轉英時，若中文前綴尾端含有「其實出自同一批按鍵」的全形標點（例如 `:` 在中文模式下被轉成全形 `：` 併入組字器），該全形標點不會被輸出成全形，而是改由輸入鍵序列承擔：
  1. 序列仍保有該半形字元時 → 自中文前綴剔除（由序列輸出半形）。
  2. 序列已因對帳而失去該鍵、但該標點確為序列所產生且仍在組字器內時 → 就地改寫為半形。
  
  故 `https` 之後接續 `://` 的輸出恆為半形的 `https://`，不會出現 `：://` 這種重複與全形殘留（此保證涵蓋實機上偶發的「序列失去 `:`、組字器仍留有 `：`」狀態）。註：序列中沒有對應半形字元的中文標點（例如先前正常輸入的「，」）一律保留。本保證對 §5.1 的三種觸發路徑一體適用。

#### C. 連續誤鍵：不需斷點鍵也自動轉英

- 情境：輸入的按鍵**個個都是合理按鍵**（沒有明顯的無效鍵），但輸入順序一路都不合理（例如聲母被覆寫的 `cdcd`、槽位倒錯的長串）。
- 規則：逐鍵累計「誤鍵」（每個新增的「不合理的中文輸入順序」違規各計一次）；當**連續誤鍵次數達到門檻**（`連續誤鍵自動轉英門檻`，可設定 **3～8 次**，預設 5）時：
  1. 當下立刻把尚未成文的中文組字內容（若有）與整段輸入鍵序列遞交為英文（**不**附加空格）。
  2. 本拍按鍵由唯音消費（其字元已含在遞交內容中）。
  3. 進入英數暫存模式。
- 計次歸零（代表「其實在打中文」）的時機：
  1. **尚未輸出英文之前按下 BackSpace**（使用者只是在修錯鍵）。
  2. 組字內容成功成文（音節固化進組字器、或任一遞交動作）。
  3. 觸發進入英數暫存模式之後。
- 註：合理的中文打字（每個音節含聲調）在每次成文時歸零，故實務上不會誤觸；若使用者刻意「無聲調連打」而累積誤鍵，亦可用 BackSpace 立刻歸零。

### 5.2 合理性檢查（輸入順序）

以當前注音排列（parser）將輸入鍵序列逐鍵模擬成一串音節（聲、介、韻、調四槽），出現下列任一情形即判定為**不合理**：

1. **無效鍵**：該鍵不是當前注音排列可吃的鍵（例如大寫字母、`'`、`"` 等）。
2. **破壞性覆寫**：聲母槽已被佔用時又鍵入聲母（例如 `cd`：`ㄏ` 尚未成音節就被 `ㄎ` 覆寫）。
3. **順序倒錯**：新鍵的槽位次序早於目前音節已填的最後一個槽位（例如 `ls`：先韻母 `ㄠ` 再聲母 `ㄋ`）。
4. **無解音節**：尚未收束的音節（聲介韻）既不是合法國語音節、也不是任何合法音節的前綴（例如 `vi`：`ㄒㄛ` 不存在，也不可能有後續）。
5. **死鍵（同鍵連按）**：同一介母／韻母鍵連按（槽位內容不變，例如 URL 的 `//` ＝ `ㄥㄥ`）；聲母槽的任何重複鍵亦屬此類。聲調鍵的重複屬既有的「聲調覆寫」語義，不計。
6. 序列中連一個注音鍵都沒有（例如只按了 `'`）時：**不**視為英文，維持既有標點行為。

音節收束規則：鍵入聲調後視為一個音節結束；下一個鍵開始新音節。聲調只計入「該音節已結束」，不參與合法性以外的判定。

**已知邊界**：若某組按鍵剛好構成合理的中文輸入順序，即使它其實是英文（例如 `cp` ＝ `ㄏㄣ`、`rm` ＝ `ㄐㄩ`），Space 規則與連續誤鍵規則亦不會觸發——這是刻意的保守設計，避免誤傷正常中文打字。這類情境請改用 **Tab**（無條件轉英），或直接切到 ABC 輸入法。

### 5.3 英數暫存模式的行為

進入英數暫存模式後，**不離開唯音**：

| 按鍵 | 行為 |
|---|---|
| 可列印 ASCII（字母、數字、符號、空白） | 原樣遞交該字元（含大寫），並刷新活動時間。 |
| BackSpace | 放行給客體刪除；更新活動時間與連續 BackSpace 計數（見 §5.4）。 |
| Tab | 放行給客體（自動完成），刷新活動時間、維持模式。 |
| Enter / Esc | 退出英數暫存模式，按鍵放行給客體。 |
| 方向鍵、翻頁鍵、功能鍵等 | 放行、刷新活動時間、維持模式。 |
| Cmd/Ctrl/Option 組合鍵 | 放行、維持模式（不干擾客體快捷鍵）。 |
| 閒置逾時 | 見 §5.5。 |

### 5.4 BackSpace 行為

- 英數暫存模式中輸入一個 BackSpace（距上次按鍵在逾時值內）＝「還在改英文」，模式不取消。
- 連續快速輸入 **3 個 BackSpace**（每次間隔皆在逾時值內）時：
  1. 本拍 BackSpace 仍放行給客體（讓自動輸出的英數內容被刪掉）。
  2. 退出英數暫存模式。
  3. **還原觸發前的注音組字進度**（注拼槽內容與輸入鍵序列），使使用者能依原意繼續打中文。
- 連續計數在輸入任何非 BackSpace 鍵時歸零；若兩次 BackSpace 之間已超過逾時值，模式本身即先因閒置而結束，該 BackSpace 由客體正常處理。

### 5.5 閒置逾時（自動回到中文模式）

- 參數：`智慧中英自動切換閒置秒數`（預設 500 ms、可由使用者設定）。
- 於英數暫存模式中，若距離上次輸入超過該毫秒數：
  - 結束英數暫存模式（不還原組字進度）。
  - **本拍按鍵**改以中文模式重新分診（等效於使用者「繼續打中文」）。

### 5.6 模式與資料邊界

- 英數暫存模式**不**切換系統輸入法、**不**碰 `isASCIIMode`（Shift 英數模式）與 Caps Lock 行為。
- 所有英文輸出皆為即時遞交；本模式不維護「未遞交的英數緩衝區」。
- 觸發時若組字器已有成文的中文，先遞交中文、再遞交英文（維持鍵入順序）。
- 三連 BackSpace 的還原僅涵蓋「尚未成文的注音組字進度」；已遞交的中文與英文內容由客體的刪除行為負責。

## 6. 狀態圖（文字描述）

```
（中文模式，輸入鍵序列持續累積）
      │
      ├── Space 且序列不合理 ──► 遞交中文(若有)+序列+空格 ─┐
      │                                                  ▼
      └── Tab 且序列非空 ─────► 遞交中文(若有)+序列 ──►（英數暫存模式）
                                                        │
    ┌───────────────────────────────────────────────────┤
    │                                                   │
    │ 可列印鍵：原樣遞交                          Enter/Esc：退出、放行
    │ BackSpace：放行＋計數                      閒置逾時：退出
    │ 3 連 BackSpace：退出＋還原注音組字進度
    ▼
（回到中文模式）
```

## 7. 偏好設定（使用者可調）

| 偏好鍵（UserDef） | 型別 | 預設 | 說明 |
|---|---|---|---|
| `SmartEnglishAutoSwitchEnabled` | Bool | `true` | 啟用智慧中英輸入自動切換。 |
| `SmartEnglishAutoSwitchIdleTimeoutMS` | Int（毫秒） | `500` | 閒置自動返回中文的毫秒數；同時作為「連續 BackSpace」的間隔上限。可選值：300 / 500 / 800 / 1000 / 1500。 |
| `SmartEnglishAutoSwitchErrorThreshold` | Int（次） | `5` | 連續誤鍵自動轉英的門檻；可選值：3 / 4 / 5 / 6 / 7 / 8。 |

設定介面位置：偏好設定 →「行為設定」面板（緊鄰「中英混打模式」開關）。

備註：本 MVP 不更動既有「以 Shift 切換中文／英數模式」的偏好預設值（假設 2 由使用者自行設定）；本功能與該模式互不干涉（`isASCIIMode` 為 true 時本功能不介入）。

## 8. MVP 範圍外（後續再議）

- 拼音鍵盤、狂拼、磁帶（Cassette）、中英混打（Mixed Alphanumerical）等模式的支援（本 MVP 僅涵蓋注音鍵盤）。
- 以字典／指令詞庫（如 `git`、`ls`）為基礎的第二層判斷。
- 英數緩衝區式（未遞交、可整段編輯）的顯示樣式、提示視窗與通知。
- 自動切換系統輸入法（TIS）或改變 Shift 模式的行為。
- 可設定的「連續 BackSpace 次數」（MVP 固定 3 次）。

## 9. 測試案例（驗收條件）

假設全程停留於「繁唯」中文模式、且功能已啟用：

| # | 輸入 | 期望 |
|---|---|---|
| 1 | `c` `d` `Space` | 客體收到 `cd `；進入英數暫存模式。 |
| 2 | 承上，續打 `.` `/`，並在逾時前按 `Enter` | 客體依序收到 `cd `、`.`、`/`；Enter 後退出模式。 |
| 3 | `l` `s` `Space` | 客體收到 `ls `（順序倒錯判定）。 |
| 4 | `v` `i` `Space` | 客體收到 `vi `（無解音節判定）。 |
| 5 | `s` `u` `3` `c` `l` `3` `Space` | 客體收到「你好 」（合理中文，不受影響）。 |
| 6 | `g` `i` `t` `Tab` | 客體收到 `git`；Tab **未被輸入法消費**（客體收到真實 Tab 按鍵、觸發自動完成）、未遞交 Tab 字元；進入英數暫存模式。 |
| 6a | `.` `/` `c` `o` `n` `Tab` | 客體收到 `./con`，其後收到真實 Tab 按鍵（放行、未遞交 Tab 字元）；進入英數暫存模式（路徑輸入情境）。 |
| 6b | `c` `o` `n` `f` `Tab` | 客體收到 `conf`，其後收到真實 Tab 按鍵（放行、未遞交 Tab 字元）；進入英數暫存模式（檔名前綴情境）。 |
| 6c | `.` `.` `/` `p` `r` `o` `Tab` | 客體收到 `../pro`，其後收到真實 Tab 按鍵（放行）。 |
| 6d | `.` `/` `D`(Shift) `o` `c` `Tab` | 客體收到 `./Doc`，其後收到真實 Tab 按鍵（放行）；大寫字母原樣保留、且只輸出一次。 |
| 6e | 案例 1 之後按 `Tab` | 僅放行真實 Tab 按鍵；不遞交任何內容（含 Tab 字元）、模式維持。 |
| 7 | 案例 1 後閒置超過逾時值，再按 `c` | 不遞交英文字元；`c` 走中文注音組字（`ㄏ` 進注拼槽）。 |
| 8 | 案例 1 後按 1 次 BackSpace | 模式維持；不遞交任何字元。 |
| 9 | 案例 1 後連按 3 次 BackSpace | 模式退出；注拼槽還原為觸發前的 `ㄎ`（輸入鍵序列亦還原），狀態回到組字顯示。 |
| 10 | 關閉偏好開關後重複案例 1 | 完全維持既有行為（不打英文）。 |
| 11 | `'` `Space`（單引號類標點） | 維持既有標點行為，不轉英文。 |
| 12 | `c` `p` `Space`（合理中文順序） | 不轉英文（維持既有中文流程）；改按 `c` `p` `Tab` 則轉為 `cp` 並送出 Tab。 |
| 13 | `.` `/` `c` `o` `n` `Space` | 客體收到 `./con `（路徑後接空格亦屬不合理注音順序）。 |
| 14 | `c` `d` `c` `d` `c` `d`（無 Space／Tab，預設門檻 5） | 第 6 鍵累計滿 5 次誤鍵 → 客體收到 `cdcdcd`（不附空格）、進入英數暫存模式；本拍按鍵被消費。 |
| 15 | `c` `d` `c` `Backspace`（尚未轉英） | 連續誤鍵計次歸零；再打 `c` `d` `c` `d` `c` `d` 才會轉英（不得重複計入退格前的違規）。 |
| 16 | 將門檻設為 3 後輸入 `c` `d` `c` `d` | 第 4 鍵達門檻 → 客體收到 `cdcd`、進入英數暫存模式。 |
| 17 | `c` `d` 之後成功組字成文（例：`2u,6` ＝ ㄉㄧㄝˊ） | 成文後連續誤鍵計次與輸入鍵序列一併歸零（確為中文打字）。 |
| 18 | 一氣呵成輸入 `h` `t` `t` `p` `s` `:` `/` `/`（預設門檻 5） | 最後一個 `/`（死鍵）達門檻 → 客體收到 `https://`、進入英數暫存模式。 |
| 19 | `,` `,` `,` `,`（快速標點巨集） | 不因死鍵新規則誤觸自動轉英（標點巨集照常運作）。 |
| 20 | `:` `/` `/` `/`（門檻 3；`:` 已被轉成全形 `：` 併入組字器） | 客體收到**半形**的 `:///`（不得為 `：:///`）；進入英數暫存模式。 |
| 21 | `:` `/` `/` `w` `w`（門檻 3；`:` 已併入組字器，但輸入鍵序列因對帳而失去 `:`） | 觸發當下遞交半形的 `://w`、續打 `w` 後累積為 `://ww`（不得出現全形 `：`）；進入英數暫存模式。 |

## 10. 風險與對策

| 風險 | 對策 |
|---|---|
| 使用者習慣「無聲調連打」或刻意倒序輸入注音時被誤判。 | 合理性判定僅在 Space／Tab 這兩種「明確斷點」上生效；其餘按鍵一律走既有注音流程；且可用偏好總開關關閉。 |
| 指令本身恰好是合理的中文輸入順序（`cp`、`rm`）。 | 屬已知邊界（§5.2）：改用 Tab，或直接切換到 ABC 輸入法。 |
| 誤轉英文後使用者想改回中文。 | 3 連 BackSpace 還原組字進度；或直接重打。 |
| 終端機以外的 App（例如中文打字中的選字窗）。 | 觸發僅在 `.ofEmpty` / `.ofInputting` 狀態；選字窗狀態不受影響。 |
| 既有偏好（中英混打、拼音、狂拼、磁帶）。 | MVP 明確互斥：僅在注音鍵盤、未啟用中英混打等條件下生效。 |
